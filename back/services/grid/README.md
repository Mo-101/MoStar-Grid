# grid/

The Neo4j substrate and the orchestrator that runs on it. Grid **executes**. Grid does not interpret intent, and Grid does not validate truth.

- `schema/` — node and relationship contracts.
- `ports/` — sovereign port registry (41xxx band).
- `components/` — GridComponent and SystemModule definitions.
- `docker/` — container manifests, compose files.
