# Handoff Note — 2026-09-02 19:24 UTC

**Session:** RuntimeManager qualification gate — replay + execution monopoly
**Agent:** Devin
**Status:** COMPLETE

---

## What was done

- Added deterministic attestation ids (UUID5) keyed on execution_id, program_hash, bytecode_hash, and abi_hash.
- Added ProvenanceStore.has_attestation() and .get_receipt() so RuntimeManager can block replays before governance.
- RuntimeManager.execute() now accepts an optional execution_id and returns REPLAY_DETECTED for duplicate executions.
- Added core/protocols/moscript/scripts/check_execution_monopoly.py to scan for unauthorized direct moscript binary invocations.
- Added tests/momind/runtime/test_execution_monopoly.py to run the static check in CI.
- Updated MOSCRIPT_RUNTIME_SECURITY_QUALIFICATION_V0_2.json to 28/28 and added honest not-implemented markers for soulprint/authorship, immutable script id, and burn/refusal.

## Why

WooGate is wired into the router; the next proof needed is a safe and traceable execution path. This commit adds the two remaining bounded runtime controls without expanding into doctrine-shaped features that have no runtime hook.

## Verification

- tests/momind/runtime/  -> 28 passed
- tests/                 -> 75 passed
- python3 core/protocols/moscript/scripts/check_execution_monopoly.py -> EXECUTION_MONOPOLY_OK

## Open items / next steps

- Bridge SemanticFrame.woo_judgment into RuntimeManager.execute() for MoScript artifact execution in the next PR.
