# Handoff Note — 2026-08-15

**Session:** Prep Grid for public deployment at grid.mostarsystems.com
**Agent:** Claude (Claude Code)
**Status:** IN PROGRESS

---

## What was done

- Switched the VPS PM2 frontend process (`deploy_grid_vps.sh`) from `vite dev`
  to a production build served via `serve.mjs`, with the build baking in
  `VITE_GRID_API_BASE=https://api.mostarsystems.com` and
  `VITE_MOSTAR_VOICE_URL=https://voice.mostarsystems.com`.
- Added `CORS_EXTRA_ORIGINS=https://grid.mostarsystems.com` to the
  `mostar-grid` PM2 env (consumed by `back/services/grid/api.py`'s existing
  CORS allowlist).
- Added `deploy/nginx/grid.mostarsystems.com.conf` — HTTP-only reverse proxy
  for `grid.` / `api.` / `voice.mostarsystems.com` to the loopback PM2 ports
  (41012 / 41010 / 41071).
- Added `deploy/setup_domain_grid.sh` — installs nginx + certbot on the VPS,
  deploys the proxy config, opens ufw 80/443. Certbot only runs if invoked
  with `RUN_CERTBOT=1`, deferred until DNS is live.

## Why

- Mostar Intelligent Systems is standardizing on `mostarsystems.com`; Grid
  gets an official production address at `grid.mostarsystems.com` (see prior
  branding sweep). This is proxy/build prep only — no rebuild, DNS change,
  or TLS issuance yet.

## Verification

- `bash -n` passed on both `deploy_grid_vps.sh` and `deploy/setup_domain_grid.sh`.
- Next: run `deploy/setup_domain_grid.sh` (no `RUN_CERTBOT`) against the VPS,
  confirm `nginx -t` passes and 80/443 open, confirm existing services are
  listening on 127.0.0.1:41010/41012/41071, confirm a frontend production
  build completes with the new API/voice URLs baked in.

## Open items / next steps

- Create A records for `grid` / `api` / `voice` in **Truehost DNS** (not
  Hostinger) pointing at 31.97.180.251 — out of band, not done yet.
- After DNS propagates: `RUN_CERTBOT=1 ./deploy/setup_domain_grid.sh`.
- Then verify all three HTTPS endpoints and the Grid CORS path end-to-end.
