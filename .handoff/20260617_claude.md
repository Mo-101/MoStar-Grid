# Handoff Note — 2026-06-17 UTC

**Session:** Render cloud deploy prep + local pm2 service diagnosis
**Agent:** Claude Sonnet 4.6
**Status:** IN PROGRESS — code/config ready, deploy itself not yet live

---

## What was done

- Diagnosed local pm2 grid services: `mostar-grid` had been stuck with
  MindGraph/DCX disconnected for 5 days (orchestrator only connects once at
  boot, never retries), and `mostar-ollama-tunnel` wasn't running at all.
- Fixed hardcoded `bolt://localhost:47687` Neo4j URIs in
  `core/engines/semantic_grid/{state,router}.py` so they resolve through
  `grid.config` (and therefore Neo4j AuraDB once configured) everywhere.
- Parameterized `back/services/mcp_gateway/api.py`'s `GRID_API_URL` and log
  file path via env vars instead of hardcoded localhost/absolute host paths.
- Made `voice_api.py`'s Piper binary/voice model paths env-configurable
  (`PIPER_BIN`, `VOICE_MODEL`) and made it bind to `$PORT`.
- Added `mindgraph_reconnect_watcher` (back/services/grid/watchers.py) and
  wired it into `api.py`'s startup tasks so a transient Neo4j outage at boot
  doesn't permanently strand the orchestrator disconnected.
- Added `edge-tts` and `feedparser` to requirements.txt — both are imported
  by `api.py`/`watchers.py` but were undeclared, only present by chance in
  the local dev venv.
- Added `Dockerfile.grid-api`, `Dockerfile.voice`, `.dockerignore`,
  `render.yaml` (Blueprint for all 4 grid services), and `runtime.txt`
  (pins Python 3.11 — Render's free Python service had defaulted to 3.14).
- Removed the tracked 1.3GB self-hosted Neo4j binary/data install at
  `back/services/mindgraph/neo4j-mindgraph/` from git (already gitignored;
  cloud deploy uses Neo4j AuraDB instead) — this cleanup was actually done
  by a prior session (see `.handoff/2026-06-12_0507_Cascade.md`) but never
  committed until now.
- Created `core/ops/scripts/validate_handoff.py` and `.handoff/TEMPLATE.md`
  — the pre-commit hook referenced both but neither existed, so the handoff
  gate was silently non-functional. It now actually validates.

## Why

User wants the grid app running full-time on Render instead of depending on
a local WSL box staying on. Neo4j is moving to Neo4j AuraDB (managed);
Ollama is moving behind a Cloudflare tunnel with a public URL. Scope for
this pass is the grid repo only — idim-ikang and crypside (separate repos,
also pm2-managed) are an explicit follow-up.

## Verification

- Confirmed locally that Neo4j (port 47687) and the proven-working Python
  venv's dependency set (`pip freeze`) match the updated requirements.txt.
- Found the existing `front/app/serve.mjs` / `deploy.sh` production flow
  (not previously visible from ecosystem.config.js, which only runs `vite
  dev`) — `render.yaml`'s frontend service now matches that flow exactly
  (`npm run build` → sync `dist/client/assets` into `public/assets` → `node
  serve.mjs`, which reads `process.env.PORT`).
- Did NOT verify the Render deploy succeeds end-to-end yet — that requires
  Aura being created and its connection string added to Render's env vars
  first (user confirmed Aura isn't set up yet).

## Open items / next steps

- User already manually created a "MoStar-Grid" Python web service in the
  Render dashboard (not from this render.yaml) — it crashed (exit 254),
  almost certainly because `NEO4J_URI`/`NEO4J_USER`/`NEO4J_PASSWORD`/
  `NEO4J_DATABASE` aren't set yet (`grid.config._required_env` raises at
  import time if missing).
- Next: user creates Neo4j AuraDB instance, adds its env vars to the
  existing Render service (or via `render.yaml` Blueprint), confirms the
  Cloudflare-tunneled Ollama URL, then redeploys.
- `git push` to `Mostar-Industries/MoStar-Grid` failed — no cached GitHub
  credentials/token in this shell, needs the user to push or supply a PAT.
