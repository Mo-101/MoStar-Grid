# Handoff Note — 2026-06-19 02:30 UTC

**Session:** Awakening boot fix, live voice registry, dashboard wired to real Grid API
**Agent:** Claude Code (Sonnet 4.6)
**Status:** COMPLETE

---

## What was done

- `awakening.tsx`: fixed a desync bug where the boot status text restarted on
  every root re-render (unstable `onComplete` in the effect deps) while the
  SVG, locked to its own one-shot CSS timeline, stayed finished — looked like
  the animation raced ahead of the text. Now reads `onComplete` via a ref so
  only `cycle` (replay) restarts the sequence.
- Consolidated the boot ring asset to a single canonical `loadord.svg` with
  working CSS keyframe animations (was a static, non-animated Figma export).
- Added live boot narration: `AwakeningScreen` now calls the MoStar Voice
  service and plays the result through a hidden `<audio>` element.
- `back/services/voice/voice_api.py`: replaced the single hardcoded
  `VOICE_MODEL` with a real `VOICES` registry (`mostar-sovereign-v1` /
  `mostar-clear-v1` / `mostar-libritts-v1`), `voice` param on `/speak`,
  unknown-id fallback + logging, `/voices` lists the registry.
- New `front/app/src/services/gridApiClient.ts`: typed client for the live
  Grid API (`/api/status`, `/api/mindgraph/status`, `/api/memory/recent`,
  `/api/semantic/advisors`).
- `parts.tsx` / `index.tsx`: replaced every hardcoded mock number on the
  dashboard (KPI counts, 12-name council roster, activity feed, health
  checks) with live data from the Grid API, with graceful
  "CONNECTING…" / "OFFLINE" fallback states.
- `elemental-toast.tsx`: element cards now import gifs directly from
  `assets/moCons/*` instead of dead Lovable-cloud asset-pointer JSON files
  that pointed at an unreachable proxy URL; cards now also overlay a live
  KPI metric.
- `council.tsx`: replaced the static orbit/avatar chamber with the
  influence-lattice visualization (ring-based node graph), nav marked
  COUNCIL active to match the route.
- `render.yaml`: added `VITE_ENABLE_LIVE_GRID_SERVICES=true` and a
  `VITE_MOSTAR_VOICE_URL` slot to the `mostar-frontend` service — without
  these the deployed build would silently stay in mock mode even with
  `VITE_GRID_API_BASE` set.

## Why

- User reported the awakening boot animation looked broken (visual finished
  before the loading text) and had no audible voice.
- User reported the dashboard showed stale/fake numbers ("everything is
  outdated") and wanted it pulling live data.
- A live Grid API (`:41010`) and Piper voice service (`:41071`) were already
  running locally but the frontend was never configured/wired to use them.

## Verification

- `tsc --noEmit` clean (only a pre-existing, unrelated dead import in
  `ui/sidebar.tsx` remains).
- Verified live against the running local backends with curl: `/api/status`,
  `/api/mindgraph/status`, `/voices`, and `/speak` with a non-default
  `voice` param all confirmed working end-to-end.
- Could not run the Vite dev server from this session's Windows/Git-Bash
  shell (native rollup module mismatch); attempted via WSL but the WSL
  Node.js is 18.19.1 and this project requires 20.19+/22.12+, so local
  dev-server verification is still pending — relying on `tsc` + live curl
  checks instead.

## Open items / next steps

- WSL's system Node (18.19.1) is too old to run `vite dev` for this project;
  needs a Node 20+/22+ install (nvm/fnm) in WSL to actually preview locally.
- Render voice image now needs redeploy so the newly bundled
  `mostar-clear-v1` and `mostar-libritts-v1` models are present in production.
- `VITE_GRID_API_BASE` and `VITE_MOSTAR_VOICE_URL` on `mostar-frontend` are
  `sync: false` — confirm the Render values point at the live Grid and Voice
  services after deploy.
