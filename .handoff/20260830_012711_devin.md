# Handoff Note — 2026-08-30 01:27 UTC

**Session:** MoMind agent canonicalization and Neo4j Mind projection hardening
**Agent:** Devin
**Status:** COMPLETE

---

## What was done

- Added `core/sovereignty/entity/migrations/agent_projection_constraint.cypher` to back `MindProjector` `MERGE` with an `a.id` uniqueness constraint.
- Froze the `mind.agent.project` MoScript capability in `MOSCRIPT_CAPABILITY_ABI_V0_2.json`; payload is restricted to `agent_id`, `canonical_hash`, `projection_template`.
- Updated `core/protocols/moscript/scrolls/README.md` so `mind_projector.ms` invokes `mind.agent.project`.
- Updated `MOMIND_INITIATION_ARCHITECTURE_STATUS.md` with the new constraint, capability, and the 10 bridge acceptance tests.

## Why

- Strengthens the Neo4j uniqueness boundary so concurrent `MERGE` projections cannot create duplicate `:Agent` identities.
- Narrows the MoScript graph capability from generic `graph.template.execute` to a single typed `mind.agent.project` request, preventing raw Cypher in `.ms` scrolls.

## Verification

- `pytest tests/momind/contracts -q` passes 47/47.
- Host-side `MindProjector` path remains fully tested: 14 canonical agents, Breda confinement, idempotency, forged/unknown denial.

## Open items / next steps

- Implement `mind.agent.project` in the Go MoScript VM and the Conduit/Py bridge; blocked on a working Go toolchain in this environment.
- Run real Neo4j projection once `NEO4J_PASSWORD` is configured.
