# tests/

- `unit/` — pure-function and module-level tests.
- `integration/` — multi-component, Neo4j-backed, Ollama-backed.
- `fixtures/` — sanitized test data. No production data. Ever.

Coverage gates land in CI once Phase 3.6 first boot is verified.
